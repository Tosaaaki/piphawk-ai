OPENAI_API_KEY=
OANDA_API_KEY=
OANDA_ACCOUNT_ID=
# OANDA_API_URL=https://api-fxpractice.oanda.com/v3
ALLOW_NO_PULLBACK_WHEN_ADX=20
BYPASS_PULLBACK_ADX_MIN=

AI_COOLDOWN_SEC_OPEN=30  # ポジション保有時のAI待機時間
AI_COOLDOWN_SEC_FLAT=15  # ノーポジション時のAI待機時間

# GPT model settings
AI_MODEL=gpt-3.5-turbo-0125          # 汎用モデル
AI_REGIME_MODEL=gpt-3.5-turbo-0125   # トレンド判定モデル
AI_ENTRY_MODEL=gpt-3.5-turbo-0125    # エントリー判断モデル
AI_EXIT_MODEL=gpt-3.5-turbo-0125     # エグジット判断モデル

# Disk guard
CLEANUP_THRESHOLD=80

# Example environment variables
# Copy this file to .env and adjust as needed
COMPOSITE_MIN=0.2
SCALP_OVERRIDE_RANGE=

# Trade mode thresholds
ATR_HIGH_RATIO=1.4
ATR_LOW_RATIO=0.8
ADX_TREND_THR=25
ADX_FLAT_THR=17

# Risk and policy
MAX_CVAR=
LOSS_LIMIT=
ERROR_LIMIT=
USE_OFFLINE_POLICY=false

# Pattern detection
USE_LOCAL_PATTERN=true
PATTERN_TFS=M5,M15
# Regime conflict settings
LOCAL_WEIGHT_THRESHOLD=0.6
IGNORE_REGIME_CONFLICT=false

# Composite mode scoring
SCALP_ENTER_SCORE=       # default 0.20
SCALP_HOLD_SCORE=        # default 0.15
RANGE_ADX_MIN=           # default 20
ADX_PERIOD=              # default 12

SCALP_AI_BBWIDTH_MAX=    # default 0 (unlimited)

# Strategy ensemble settings
STRAT_TEMP=0.15
STRAT_N=3
STRAT_VOTE_MIN=2

# Entry vertical buffer
ENTRY_BUFFER_K=3

# Rule-based regime detection
REGIME_ADX_TREND=30
REGIME_BB_NARROW=0.05

# 強制エントリーフラグ
FORCE_ENTER=true
# AI 判断後はフィルタを無視して注文するか
FORCE_ENTRY_AFTER_AI=true

# Atmosphere module settings
ATMOS_EMA_WEIGHT=0.4
ATMOS_RSI_WEIGHT=0.3
ATMOS_THRESHOLD=0.5

OPENAI_API_KEY=
OANDA_API_KEY=
OANDA_ACCOUNT_ID=
# OANDA_API_URL=https://api-fxpractice.oanda.com/v3
ALLOW_NO_PULLBACK_WHEN_ADX=
BYPASS_PULLBACK_ADX_MIN=

AI_COOLDOWN_SEC=30

# GPT model settings
AI_MODEL=gpt-4o-nano          # 汎用モデル
AI_REGIME_MODEL=gpt-4o-nano   # トレンド判定モデル
AI_ENTRY_MODEL=gpt-4o-nano    # エントリー判断モデル
AI_EXIT_MODEL=gpt-4o-nano     # エグジット判断モデル

# Disk guard
CLEANUP_THRESHOLD=80

# Example environment variables
# Copy this file to .env and adjust as needed
COMPOSITE_MIN=0.2
SCALP_OVERRIDE_RANGE=

# Trade mode thresholds
ATR_HIGH_RATIO=1.4
ATR_LOW_RATIO=0.8
ADX_TREND_THR=25
ADX_FLAT_THR=17

# Risk and policy
MAX_CVAR=
LOSS_LIMIT=
ERROR_LIMIT=
USE_OFFLINE_POLICY=false

# Pattern detection
USE_LOCAL_PATTERN=true
PATTERN_TFS=M5,M15

# Composite mode scoring
SCALP_ENTER_SCORE=       # default 0.20
SCALP_HOLD_SCORE=        # default 0.15
RANGE_ADX_MIN=           # default 20
ADX_PERIOD=              # default 12

SCALP_AI_BBWIDTH_MAX=    # default 0 (unlimited)
